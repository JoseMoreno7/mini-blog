{
  "name": "jose/mini-blog",
  "description": "Mini Blog PHP",
  "require": {
    "php": "^8.2"
  },
  "scripts": {
    "post-install-cmd": [
      "docker-php-ext-install pdo pdo_mysql"
    ],
    "start": "php -S 0.0.0.0:${PORT:-8080} -t public"
  }
}
